// Bytebeat track metadata!
// These songs are all stolen directly from https://dollchan.net/bytebeat/ >:)
window.BYTEBEAT_TRACKS = Object.freeze({
  normal: Object.freeze({
    mode: 'floatbeat',
    sampleRate: 32000,
    source: 'assets/audio/music/normal.txt',
  }),

  boss: Object.freeze({
    mode: 'floatbeat',
    sampleRate: 48000,
    source: 'assets/audio/music/boss.txt',
  }),

  shop: Object.freeze({
    mode: 'floatbeat',
    sampleRate: 64000,
    source: 'assets/audio/music/shop.txt',
  }),

  gameover: Object.freeze({
    mode: 'floatbeat',
    sampleRate: 44100,
    source: 'assets/audio/music/gameover.txt',
  }),

  multiverseComplete: Object.freeze({
    mode: 'floatbeat',
    sampleRate: 25941.33203125,
    source: 'assets/audio/music/multiverse-complete.txt',
  }),
});